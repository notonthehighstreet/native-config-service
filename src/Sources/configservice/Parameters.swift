import Foundation

public struct Parameters {
  
  public enum Route : String {
    case application  = "application"
    case branch       = "branch"
    case device       = "device"
    case health       = "health"
    case v1           = "v1"
    case config       = "config"
  }
  
}
